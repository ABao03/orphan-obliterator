Request signals -> calling the parent to provide a certain variable.
Send signals -> called by request only, not called by the parent. sends a certain variable to a different node.

All assets must be png
